# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files (contain secrets — never commit)
.env
.env.local
.env.*

# Lokalt token-cache (innehåller OAuth-tokens – ska inte committas)
server/tokens.json

# Lokal agent-körhistorik och spend-logg (operationell data per maskin)
server/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Bundled API for Vercel (generated by npm run build)
api/server.bundle.mjs

.vercel

# Templates are meant to be committed
!.env.example
