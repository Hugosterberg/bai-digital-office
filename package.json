{
  "name": "bai-digital-office",
  "private": true,
  "scripts": {
    "dev": "npm --prefix office run dev",
    "build": "npm --prefix office run build",
    "verify": "npm --prefix office run verify",
    "start": "npm --prefix office run worker"
  }
}
