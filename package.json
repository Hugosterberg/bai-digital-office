{
  "name": "bai-digital-office",
  "private": true,
  "scripts": {
    "dev": "npm --prefix mission-control run dev",
    "build": "npm --prefix mission-control run build",
    "verify": "npm --prefix mission-control run verify"
  }
}
